# Comma-separated URLs to ping. First URL = homepage (hit first each cycle).
# Default: https://www.parlaygorilla.com/,https://api.parlaygorilla.com/health
TARGET_URLS=https://www.parlaygorilla.com/,https://api.parlaygorilla.com/health
# URLs to hit immediately after homepage (e.g. deep health for DB warmup). Default: https://api.parlaygorilla.com/health?deep=1
POST_LOAD_URLS=https://api.parlaygorilla.com/health?deep=1
# Seconds between ping cycles. Default: 600
INTERVAL_SECONDS=600
# Request timeout in seconds. Default: 15
TIMEOUT_SECONDS=15
# Retries on failure/timeout/5xx. Default: 2
RETRIES=2
# Seconds to wait between retries. Default: 3
BACKOFF_SECONDS=3
# Consecutive failures before Telegram alert. Default: 3
ALERT_CONSECUTIVE_FAILURES=3
# Latency (ms) above which to alert (throttled once/hour per URL). Default: 4000
ALERT_LATENCY_MS=4000
# Optional Telegram bot token and chat ID for alerts
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
